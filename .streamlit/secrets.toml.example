# =============================================================================
# 📧 SMTP 설정 파일
# =============================================================================
# 이 파일에 SMTP 정보를 입력하면 앱에서 자동으로 불러옵니다.
# ⚠️ 주의: 이 파일은 절대 GitHub에 업로드하지 마세요!
# =============================================================================

# SMTP 서버 설정
SMTP_SERVER = "smtp.hiworks.com"
SMTP_PORT = 465

# 인증 정보 (본인의 정보로 수정하세요)
SMTP_ID = "your_email@company.com"
SMTP_PW = "your_password"

# 발신자 정보
SENDER_NAME = "발신자 이름"

# SMTP 제공자 (hiworks, gmail, naver, custom 중 선택)
SMTP_PROVIDER = "hiworks"
