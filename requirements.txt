# =============================================================================
# 📧 지능형 그룹핑 메일머지 시스템 - Dependencies
# =============================================================================
# 설치 명령: pip install -r requirements.txt
# =============================================================================

# Core Framework
streamlit>=1.28.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0        # Excel .xlsx 파일 읽기/쓰기
xlrd>=2.0.0            # Excel .xls 파일 읽기 (레거시)

# Template Engine
Jinja2>=3.1.0

# Data Validation
pydantic>=2.0.0
email-validator>=2.0.0  # 이메일 형식 검증

# Utilities
python-dateutil>=2.8.0  # 날짜 파싱

# Cookie Management
extra-streamlit-components>=0.1.60  # 쿠키 기반 설정 저장

# Visualization (History Dashboard)
plotly>=5.18.0          # 통계 차트

# =============================================================================
# 📌 참고: 아래 라이브러리는 Python 표준 라이브러리 (설치 불필요)
# =============================================================================
# smtplib          - SMTP 이메일 발송
# ssl              - SSL/TLS 암호화
# socket           - 네트워크 연결
# email.mime.*     - 이메일 메시지 구성
# =============================================================================
